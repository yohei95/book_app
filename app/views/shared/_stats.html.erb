<% @user ||= current_user %>
<div class="stats">
  <a href="<%= follows_user_path(@user) %>">
    <strong id="following" class="stat"><%= @user.following.count %></strong>人をフォロー
  </a>　
  <a href="<%= followers_user_path(@user) %>">
    <strong id="followers" class="stat"><%= @user.followers.count %></strong>人のフォロワー
  </a>
</div>