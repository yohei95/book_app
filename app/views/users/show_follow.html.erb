<% provide(:title, @title) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">

  <%# <% if @user.image.attached? %> 
    <%# <%= image_tag @user.image.variant(resize: '50x50') %> 
  <%# <% else %> 
    <%# <%= image_tag 'noimageman.png', :size =>'50x50' %> 
  <%# <% end %> 

      <h1><%= @user.name %></h1>
      <span><%= link_to "プロフィール", @user %></span>
      <span><b>投稿</b><%= @user.posts.count %><b>件</b></span>
    </section>
    <section class="stats">
      <%= render 'shared/stats' %>
    </section>
  </aside>
  <div class="col-md-8">
    <h3><%= @title %></h3>
    <% if @users.any? %>
      <ul class="users follow">
        <%= render @users %>
      </ul>
      <%= will_paginate %>
    <% end %>
  </div>
</div>